#include "function.h"

int main(void) {

    MazeBoard *M1 = (MazeBoard *)malloc(sizeof(MazeBoard));
    Stack *S1 = (Stack *)malloc(sizeof(Stack));
    StackInit(S1);
    ShowMap(M1);

    while(TRUE) {


    }
}
